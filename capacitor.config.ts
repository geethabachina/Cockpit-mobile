import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // appId: 'com.example.app',
  appId: 'com.coexample.coapp',
  appName: 'Cockpit',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
